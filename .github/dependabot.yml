version: 2
updates:
    # Check for updates to rust packages every week
    - package-ecosystem: cargo
      directory: "/"
      schedule:
          interval: weekly

    # Check for updates to GitHub Actions every month
    - package-ecosystem: "github-actions"
      directory: "/"
      schedule:
          interval: "monthly"
